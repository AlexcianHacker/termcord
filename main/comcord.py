import aiohttp; 
import discord; 
from discord.ext import commands; 
import requests; 


def main() -> int: 
  def initialize_tk(token: str, 
                    prefix: str = None) -> int: 
    
    return 
  def initialize_login(eml: str, 
                      pswd: str, 
                      prefix: str = None, 
                      token: str = None) -> int: 
    
    return 